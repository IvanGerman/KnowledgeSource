import React from 'react';
import styles from './Link.module.css';

const Link = (props) => {  

  return (
    <div  className = { styles.wrapper }>
       Link
    </div>
  )
};

export default Link;
