import React from 'react';
import styles from './Text.module.css';

const Text = (props) => {  

  return (
    <div  className = { styles.wrapper }>
       Text
    </div>
  )
};

export default Text;
